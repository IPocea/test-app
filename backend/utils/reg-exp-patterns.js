const checkIfHasDigitsPattern = /\d/g;
const checkIfHasNumbersOnlyPattern = /^(-?\d*\.?\d+|\d+\.\d*)$/;

const REG_EXP_PATTERNS = {
  checkIfHasDigitsPattern,
  checkIfHasNumbersOnlyPattern,
};

module.exports = REG_EXP_PATTERNS;
