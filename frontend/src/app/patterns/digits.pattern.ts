export const NO_DIGITS_PATTERN: RegExp = /^\D*$/;
export const DIGITS_ONLY_PATTERN: RegExp = /^(-?\d*\.?\d+|\d+\.\d*)$/;
export const FIRST_CNP_DIGIT_PATTERN: RegExp = /^[1-6]\d*$/;
export const MAX_4_DIGITS: RegExp = /^[0-9]{1,4}$/;