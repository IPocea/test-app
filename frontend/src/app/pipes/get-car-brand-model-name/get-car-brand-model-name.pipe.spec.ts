import { GetCarBrandModelNamePipe } from './get-car-brand-model-name.pipe';

describe('GetCarBrandModelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCarBrandModelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
